name: Release

on:
  push:
    tags:
      - "v*"
  workflow_dispatch:
    inputs:
      tag:
        description: Existing release tag to validate and publish
        required: true
        type: string

permissions:
  contents: write

concurrency:
  group: release-${{ inputs.tag || github.ref }}
  cancel-in-progress: false

env:
  RELEASE_TAG: ${{ inputs.tag || github.ref_name }}

jobs:
  macos-bundle:
    name: Build standalone macOS arm64 bundle
    runs-on: macos-15
    steps:
      - uses: actions/checkout@v7
        with:
          ref: ${{ env.RELEASE_TAG }}
      - uses: actions/setup-python@v7
        with:
          python-version: "3.13"
          cache: pip
      - name: Build wheel
        run: |
          python -m pip install --upgrade pip 'build>=1.5,<2'
          python -m build --wheel
      - name: Build and smoke-test standalone bundle
        run: scripts/build_macos_bundle.sh dist/*.whl artifacts
      - uses: actions/upload-artifact@v6
        with:
          name: macos-arm64
          path: artifacts/*.tar.gz
          if-no-files-found: error

  release:
    name: Publish GitHub release
    runs-on: ubuntu-latest
    needs: macos-bundle
    steps:
      - uses: actions/checkout@v7
        with:
          ref: ${{ env.RELEASE_TAG }}
      - uses: actions/setup-python@v7
        with:
          python-version: "3.14"
          cache: pip
      - name: Verify tag matches package version
        run: |
          PACKAGE_VERSION=$(python -c 'import tomllib; print(tomllib.load(open("pyproject.toml", "rb"))["project"]["version"])')
          test "$RELEASE_TAG" = "v$PACKAGE_VERSION"
      - name: Install release tools
        run: python -m pip install --upgrade pip 'build>=1.5,<2' 'twine>=6.2,<7'
      - name: Build distributions
        run: python -m build
      - name: Validate package metadata
        run: python -m twine check dist/*
      - name: Smoke-test wheel
        run: |
          python -m venv /tmp/cyberdeck-release-test
          /tmp/cyberdeck-release-test/bin/python -m pip install dist/*.whl
          test "$(/tmp/cyberdeck-release-test/bin/cyberdeck --version)" = "cyberdeck ${RELEASE_TAG#v}"
      - uses: actions/download-artifact@v7
        with:
          name: macos-arm64
          path: dist
      - name: Generate checksums
        run: (cd dist && sha256sum *) > SHA256SUMS
      - name: Create GitHub release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          if ! gh release view "$RELEASE_TAG" > /dev/null 2>&1; then
            gh release create "$RELEASE_TAG" --verify-tag --generate-notes --title "Cyberdeck $RELEASE_TAG"
          fi
          gh release upload "$RELEASE_TAG" dist/* SHA256SUMS --clobber
      - name: Dispatch Homebrew tap update
        env:
          GH_TOKEN: ${{ secrets.HOMEBREW_TAP_TOKEN }}
        run: |
          VERSION="${RELEASE_TAG#v}"
          SOURCE_ARCHIVE="cyberdeck_tui-${VERSION}.tar.gz"
          SOURCE_SHA256=$(awk -v archive="$SOURCE_ARCHIVE" '$2 == archive { print $1 }' SHA256SUMS)
          test -n "$SOURCE_SHA256"
          SOURCE_URL="https://github.com/${GITHUB_REPOSITORY}/releases/download/${RELEASE_TAG}/${SOURCE_ARCHIVE}"
          MACOS_ARCHIVE="cyberdeck-${VERSION}-macos-arm64.tar.gz"
          MACOS_SHA256=$(awk -v archive="$MACOS_ARCHIVE" '$2 == archive { print $1 }' SHA256SUMS)
          test -n "$MACOS_SHA256"
          MACOS_URL="https://github.com/${GITHUB_REPOSITORY}/releases/download/${RELEASE_TAG}/${MACOS_ARCHIVE}"
          jq -n \
            --arg version "$VERSION" \
            --arg tag "$RELEASE_TAG" \
            --arg source_url "$SOURCE_URL" \
            --arg source_sha256 "$SOURCE_SHA256" \
            --arg macos_arm64_url "$MACOS_URL" \
            --arg macos_arm64_sha256 "$MACOS_SHA256" \
            '{
              event_type: "cyberdeck_release",
              client_payload: {
                version: $version,
                tag: $tag,
                source_url: $source_url,
                source_sha256: $source_sha256,
                macos_arm64_url: $macos_arm64_url,
                macos_arm64_sha256: $macos_arm64_sha256
              }
            }' > "$RUNNER_TEMP/homebrew-dispatch.json"
          gh api \
            --method POST \
            repos/jessecanderson/homebrew-tap/dispatches \
            --input "$RUNNER_TEMP/homebrew-dispatch.json"
